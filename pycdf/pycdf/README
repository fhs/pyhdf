The pycdfext module can be compiled for use either with 
'numpy', 'numarray' or 'numeric'. To compile the module :

  % cd pycdfext/numarray  # to use numarray
  % cd pycdfext/numeric   # to use numeric
  % cd pycdfext/numpy     # to use numpy

Then :
  % make python

The following wrapper files will be generated :

  pycdfext_wrap.c    # C extension module
  pycdfext.py        # python code wrapping the extension module

This directory holds a 3rd file 'pycdfext_array.py'. This is a module 
which executes the imports which depend on the array package selected at install
time. Those imports will in turn be imported by 'pycdf.py'. 

Run "python setup.py install" from the installation root directory
to compile and install the module.
